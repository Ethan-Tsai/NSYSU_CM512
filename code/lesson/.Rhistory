Sys.time()
nrow(available.packages())
iris
nrow(available.packges())
a
const=50
prob=c(0.5,0.2,0.3)
sample=c(50,-10,80)
prob*sample
a.payoff=prob*sample
a=prob*sample
a.payoff=sum(prob*sample)
a=prob*sample
a
attitude
a.payoff=sum(prob*sample)
payoff=c(50,-10,80)
Sys.time()
nrow(available.packages())
const=50
prob=c(0.5,0.2,0.3)
payoff=c(50,-10,80)
a=prob*sample
a.payoff=sum(prob*sample)
a.payoff=sum(prob*sample)
Sys.time()
nrow(available.packages())
const=50
prob=c(0.5,0.2,0.3)
payoff=c(50,-10,80)
a=prob*payoff
a.payoff=sum(prob*payoff)
Sys.time()
nrow(available.packages())
const=50
prob=c(0.5,0.2,0.3)
payoff=c(50,-10,80)
a=prob*payoff
a=prob*payof
